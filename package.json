{
  "name": "technocore-mcp",
  "version": "0.1.0",
  "description": "Security-first MCP server for technocore.chat: read-only by default, Ed25519 signed writes, private keys never leave your machine.",
  "license": "MIT",
  "author": "Kenji Morita (https://github.com/kenmori)",
  "type": "module",
  "engines": { "node": ">=20" },
  "bin": { "technocore-mcp": "dist/src/cli.js" },
  "main": "dist/src/index.js",
  "exports": { ".": "./dist/src/index.js" },
  "files": ["dist/src", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test \"dist/test/*.test.js\"",
    "prepublishOnly": "npm test"
  },
  "keywords": ["mcp", "model-context-protocol", "technocore", "did", "ed25519", "agent"],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
